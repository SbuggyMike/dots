<!-- SC_OFF --><div class="md"><p>I have a configuration where:</p> <ul> <li><code>c-basic-offset</code> is <code>4</code></li> <li>in <code>c-offsets-alist</code>, <code>statement</code> still has the default value of <code>0</code></li> <li><p>in <code>c-offsets-alist</code>, <code>cpp-define-intro</code> set such that strings don&#39;t have to be indented, like:</p> <pre><code>#define MYSTR \ &quot;foo&quot; </code></pre></li> </ul> <p>So why do subsequent lines in multi-line strings get a 1-char indent:</p> <pre><code>#define MYSTR \ &quot;foo\n&quot;\ &quot;bar&quot; </code></pre> <p>And I guess more importantly: How can I make Emacs not want to insert those spaces?</p> <p>(The goal is to configure Emacs to match the existing style of this existing codebase.)</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/LukeShu"> /u/LukeShu </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/138dr0c/adjusting_the_indentation_of_c_macros/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/138dr0c/adjusting_the_indentation_of_c_macros/">[comments]</a></span>