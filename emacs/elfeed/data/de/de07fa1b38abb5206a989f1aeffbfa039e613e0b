<!-- SC_OFF --><div class="md"><p>I changed <code>cl-pushnew</code> to <code>pushnew</code> in a file which used it and it worked.</p> <p>After looking at the info it seems that there is some conflict between <code>cl</code> and <code>cl-lib</code> and having both installed seems to the problem.</p> <p>The conclusion for me is that although <code>cl-pushnew</code> is defined in <code>cl-lib</code> the loading of <code>cl</code> seemed to void it in some way.</p> <p>Every now and then I get a warning that I should use <code>cl is deprecated</code>. How do I get rid of it? I don&#39;t think I have <code>require&#39;d</code> it in my own code, but other libraries seem to depend on it.</p> <p><strong>Info on pushnew</strong></p> <pre><code>pushnew is an alias for ‘cl-pushnew’ in ‘cl.el’. (pushnew X PLACE [KEYWORD VALUE]...) Add X to the list stored in PLACE unless X is already in the list. PLACE is a generalized variable that stores a list. Like (push X PLACE), except that PLACE is unmodified if X is ‘eql’ to an element already in the list stored in PLACE. Keywords supported: :test :test-not :key </code></pre> <p><strong>Info on cl-pushnew</strong> </p> <pre><code>cl-pushnew is a Lisp macro in ‘cl-lib.el’. (cl-pushnew X PLACE [KEYWORD VALUE]...) Add X to the list stored in PLACE unless X is already in the list. PLACE is a generalized variable that stores a list. Like (push X PLACE), except that PLACE is unmodified if X is ‘eql’ to an element already in the list stored in PLACE. Keywords supported: :test :test-not :key [back] [forward] </code></pre> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/vfclists"> /u/vfclists </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/whmpq9/why_should_pushnew_work_in_a_program_but_not/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/whmpq9/why_should_pushnew_work_in_a_program_but_not/">[comments]</a></span>