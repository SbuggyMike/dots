<!-- SC_OFF --><div class="md"><p><strong>Distro</strong>: Manjaro i3 community</p> <blockquote> <p><strong><em>Problem:</em></strong> <code>rm</code> command picks the <code>-</code> and thinks i am giving it an argument</p> </blockquote> <p><strong>Solutions I have tried</strong>: i tried to remove that file using the <code>\</code> back slash to escape the <code>-</code> but rm picks it up again i tried to wrap it up in the <code>&#39;</code> single quotes but does not work</p> <p>this is the devil file name <code>-dev-null</code> and it is located in my home directory <code>~/</code></p> <p><em>For anyone think remove it using the gui file manager i am not going to do that because i want to know why rm is not doing what i am saying it to do</em></p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/theSafdarAwan"> /u/theSafdarAwan </a> <br/> <span><a href="https://www.reddit.com/r/archlinux/comments/wb9500/help_neede_linux_is_trolling_me_its_not_allowing/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/archlinux/comments/wb9500/help_neede_linux_is_trolling_me_its_not_allowing/">[comments]</a></span>