<!-- SC_OFF --><div class="md"><p>I started using <a href="https://github.com/redguardtoo/vc-msg">https://github.com/redguardtoo/vc-msg</a> which is awesome. But often I would like to jump to the pr not just the commit. I found a way to do so by taking the data from git-commit-link and running it through a simple bash script</p> <pre><code>#!/usr/bin/env bash git_url=$1 echo $git_url | sed &#39;s/https:\/\/github.com/repos/&#39; | sed &#39;s/commit/commits/&#39; | sed &#39;s/$/\/pulls/&#39; | xargs gh api | jq -r .[].html_url | xargs xdg-open </code></pre> <p>but I can&#39;t figure out how to write the elisp so i can call it from the ui.</p> <pre><code>(eval-after-load &#39;vc-msg-git &#39;(progn (push &#39;(&quot;p&quot; &quot;[p]r open pr on github&quot; (lambda () (let* ((info vc-msg-previous-commit-info)) (with-temp-buffer (insert (plist-get info :id)) (call-interactively &#39;git-link-commit))))) vc-msg-git-extra))) </code></pre> <p>i have that written i feel like i just need to wrap part of it in a (shell-command) but havent been able to get it working.</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/mogorman84"> /u/mogorman84 </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/w5hcv8/help_with_elisp_to_jump_to_a_pr/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/w5hcv8/help_with_elisp_to_jump_to_a_pr/">[comments]</a></span>