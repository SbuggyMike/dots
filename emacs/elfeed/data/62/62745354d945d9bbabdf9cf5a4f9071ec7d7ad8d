<!-- SC_OFF --><div class="md"><p>Foot terminal ships with two systemd units.</p> <p><code>systemctl --user list-unit-files | rg foot</code>:</p> <pre><code>foot-server@.service disabled enabled foot-server@.socket disabled enabled </code></pre> <p><em>Running the above without</em> <code>--user</code> <em>produces nothing.</em></p> <p><code>systemctl --user start foot-server@</code>:</p> <pre><code>Failed to start foot-server@.service: Unit name foot-server@.service is missing the instance name. See user logs and &#39;systemctl --user status foot-server@.service&#39; for details. </code></pre> <p><code>systemctl --user status</code> [<code>foot-server@.service</code>](mailto:<a href="mailto:foot-server@.service">foot-server@.service</a>):</p> <pre><code>Failed to get properties: Unit name foot-server@.service is neither a valid invocation ID nor unit name. </code></pre> <p><code>journalctl --user-unit=foot-server@</code> produces nothing.</p> <p>Thanks</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/Pound_Hash"> /u/Pound_Hash </a> <br/> <span><a href="https://www.reddit.com/r/archlinux/comments/w2e05x/how_to_enable_foots_systemd_service/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/archlinux/comments/w2e05x/how_to_enable_foots_systemd_service/">[comments]</a></span>