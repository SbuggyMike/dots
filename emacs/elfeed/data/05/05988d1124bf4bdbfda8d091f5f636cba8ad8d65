<!-- SC_OFF --><div class="md"><p>For instance I create a sample script with the following function:</p> <p><code> (defun hello-world() (message &quot;Hello world!&quot;)) </code></p> <p>On the next line I put opening parenthesis <code>(</code>, type <code>hello-</code> and press <code>C-M-i</code> but <code>No match</code> message is shown. If I return back and evaluate function explicitly via <code>C-X C-E</code> autocomplete works for <code>hello-</code> string. But it is not convenient. For instance in Sublime Text when writing in Python I just need to declare some function to be able complete it&#39;s name. Is it possible to do in ELisp? I don&#39;t want to reevaluate all lisp code every time I want use any declared thing in it.</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/EmilySeville7cfg"> /u/EmilySeville7cfg </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/wbkbwd/how_to_automatically_enable_completions_for_just/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/wbkbwd/how_to_automatically_enable_completions_for_just/">[comments]</a></span>