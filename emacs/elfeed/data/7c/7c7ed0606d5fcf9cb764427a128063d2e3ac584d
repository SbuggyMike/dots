<!-- SC_OFF --><div class="md"><p>Hi, I&#39;m new to emacs and have been working my way through various Info manuals I find interesting. Right now I&#39;m on the &quot;eintr&quot; one (the intro to Lisp one). In section 4.1 &quot;Finding More Information&quot;, it says the following:</p> <pre><code>For example, ‘xref-find-definitions’ will jump to the various nodes in the Texinfo source file of this document (provided that you’ve run the ‘etags’ utility to record all the nodes in the manuals that come with Emacs; see (emacs)Create Tags Table). </code></pre> <p>When I try the command <strong>M-.</strong> it puts this in the minibuffer:</p> <pre><code>Visit tags table (default TAGS): /usr/local/Cellar/emacs/28.1/share/info/emacs/ </code></pre> <p>Which I interpret to mean I do not have a TAGS file already generated for the Info pages.</p> <p>If I navigate to:</p> <pre><code>/usr/local/Cellar/emacs/28.1/share/info/emacs </code></pre> <p>And then do:</p> <pre><code>etags eintr.info.gz </code></pre> <p>It does create a TAGS file, but it doesn&#39;t look right. Every line starts with &quot;function&quot;, which makes me think etags interpreted this as the wrong language. I&#39;m assuming the language expected is texinfo (based on the Info pages own text), so I tried manually specifying the language:</p> <pre><code>etags -l texinfo eintr.info.gz </code></pre> <p>But it creates an almost-empty TAGS file.</p> <p>Now I&#39;m kind of stumped. Anyone have any ideas how I can get tags working within Info mode? Thanks!</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/snailiens"> /u/snailiens </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/wdu0c3/how_to_generate_tags_table_for_info_pages_macos/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/wdu0c3/how_to_generate_tags_table_for_info_pages_macos/">[comments]</a></span>