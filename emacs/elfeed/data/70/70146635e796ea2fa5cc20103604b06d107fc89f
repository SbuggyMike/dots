<!-- SC_OFF --><div class="md"><p>I know this is tangential to Emacs but it probably will affect other Emacs users so I though it&#39;d be alright to post it here.</p> <p>I usually build Emacs from source every so often, I tried today after pulling and got a strange issue during the <code>make</code> process.</p> <p>I&#39;ll spare you the super long macro unraveling but I got this issue: </p> <pre><code>Loading macroexp.elc... Internal native compiler error: &quot;../../lisp/international/titdic-cnv.el&quot;, &quot;failing to compile function&quot;, #&lt;symbol titdic- convert at 16545&gt;, &quot;gcc_jit_context_new_call_through_ptr: not enough arguments to fn_ptr: freloc- &gt;R6e6172726f772d746f2d726567696f6e_narrow_to_region_0 (got 2 args, expected 3)&quot; </code></pre> <p>and several similar issues related to <code>gcc_jit_context_new_call_through_ptr</code> not receiving the proper number of arguments.</p> <p>This happened with:</p> <ul> <li>Emacs pulled today (July 29th 2022)</li> <li>libgccjit 12.1.0-3 (on Manjaro Linux, installed with <code>sudo pacman -S libgccjit</code>)</li> <li>gcc 12.1.0-3 (on Manjaro Linux, installed with the package manager as well)</li> </ul> <p>Since I&#39;m not seeing posts everywhere of people failing to build from source, I expect this is an issue with <code>libgccjit</code>. </p> <p>Can anyone replicate this behavior and has any tips to fix it?</p> <p>Thanks for the help.</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/NoisyFrequency"> /u/NoisyFrequency </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/wb4r6o/libgccjit_issues_when_building_from_source/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/wb4r6o/libgccjit_issues_when_building_from_source/">[comments]</a></span>