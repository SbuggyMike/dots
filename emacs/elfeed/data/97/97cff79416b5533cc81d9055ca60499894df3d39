<!-- SC_OFF --><div class="md"><p>Hi all. I am using tabspaces: <a href="https://github.com/mclear-tools/tabspaces">https://github.com/mclear-tools/tabspaces</a></p> <p>I tried to set a custom shortcut for the command <code>tabspaces-switch-to-buffer</code>. </p> <p>I have this:</p> <pre><code>(global-set-key (kbd &quot;M-b&quot;) &#39;tabspaces-switch-to-buffer) </code></pre> <p>and got the error: wrong type argument, commandp. This is weird because I bind the <code>tab-next</code></p> <p>and <code>tab-previous</code> in exactly the same fashion. </p> <p>&#x200B;</p> <pre><code>(global-set-key (kbd &quot;C-&lt;left&gt;&quot;) &#39;tab-previous) (global-set-key (kbd &quot;C-&lt;right&gt;&quot;) &#39;tab-next) </code></pre> <p>So I am not sure why it gives the error. The command when run via <code>M-x</code> works fine.</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/maxle0312"> /u/maxle0312 </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/w1uj7t/how_to_set_keyboard_shortcut_wrong_type_argument/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/w1uj7t/how_to_set_keyboard_shortcut_wrong_type_argument/">[comments]</a></span>