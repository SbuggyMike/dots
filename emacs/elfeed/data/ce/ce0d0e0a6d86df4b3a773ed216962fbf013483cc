<!-- SC_OFF --><div class="md"><p>My <code>init.el</code> has only one line:</p> <pre><code>(set-face-attribute &#39;isearch nil :font &quot;Helvetica&quot;) </code></pre> <p>To reproduce the issue, run:</p> <pre><code>emacs -q -l init.el </code></pre> <p>Then press like this to zoom:<code>C-x C-= = = = =</code></p> <p>Then do isearch like this: <code>C-s GNU</code></p> <p>The highlighted text &quot;GNU&quot; appears to be small in comparison with the surrounding zoomed text. I am using mac by the way. Are you able to reproduce this? How to resolve this so that the isearch highlighted text also grows in size as the buffer is zoomed?</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/biggrben"> /u/biggrben </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/wefg2g/custom_font_does_not_increase_in_size_when_i_zoom/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/wefg2g/custom_font_does_not_increase_in_size_when_i_zoom/">[comments]</a></span>