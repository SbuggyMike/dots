<!-- SC_OFF --><div class="md"><p>Hi, I tried everything.</p> <p>I have <code>org</code> setted up with <code>elpaca</code> like this:</p> <p>```elisp (use-package org :config (font-lock-add-keywords &#39;org-mode &#39;((&quot;^ *\([-]\) &quot; (0 (prog1 () (compose-region (match-beginning 1) (match-end 1) &quot;•&quot;)))))) (electric-indent-local-mode -1) (org-babel-do-load-languages &#39;org-babel-load-languages &#39;((clojure . t) (cpp . t) (elisp . t) (haskell . t) (python . t) (scheme . t) (shell . t) (C . t))) (require &#39;ob-python) :hook ((org-mode . org-indent-mode) (org-mode . variable-pitch-mode) (org-mode . visual-line-mode)) :init (setq org-agenda-files &#39;(&quot;~/Notes&quot;) org-babel-default-header-args:python &#39;((:results . &quot;output&quot;)) org-confirm-babel-evaluate nil org-edit-src-content-indentation 2 org-ellipsis &quot;  &quot; org-fontify-quote-and-verse-blocks t org-hide-block-startup nil org-hide-emphasis-markers t org-image-actual-width nil org-pretty-entities t org-src-fontify-natively t org-src-preserve-indentation t))</p> <p>```</p> <p>I tried moving <code>org-babel-do-load-languages</code> to <code>:init</code>.</p> <p>I tried moving it at the end of my configuration after <code>(elpaca-wait)</code>.</p> <p>I tried putting it inside <code>(eval-after-load &#39;org)</code>.</p> <p>If I go to an Org file (and I see that the configuration is getting loaded, because things like <code>org-agenda-files</code> appear), and I do <code>C-c C-c</code> on a Python source file, I receive the message <code>No org-babel-execute function for python!</code>.</p> <p>The only way it works is if I do <code>ielm</code> and either manually do the <code>org-babel-do-load-languages</code> thing, or I run <code>(require &#39;ob-python)</code>.</p> <p>It&#39;s been all day and I can&#39;t get it to work inside my configuration file.</p> <p>Can somebody help me please?</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/sicr0"> /u/sicr0 </a> <br/> <span><a href="https://www.reddit.com/r/emacs/comments/131e7ci/i_cant_get_orgbabel_to_work/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/emacs/comments/131e7ci/i_cant_get_orgbabel_to_work/">[comments]</a></span>