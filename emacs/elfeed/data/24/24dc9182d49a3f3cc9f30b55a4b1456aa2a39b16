<!-- SC_OFF --><div class="md"><p>suspect got lost in &quot;4. Setup bootloader&quot;.so need to change a bit of instruction for the wiki, since &quot;bootctl --path=/boot install&quot; doesn&#39;t work for me, but &quot;bootctl --path=/mnt/boot install &quot; &quot;work? not sure, at least no error and installed&quot;; the reason using &quot; bootctl --path=/mnt/boot install&quot; to matchs previous instruction of</p> <p>&quot; First, make sure you mounted the EFI System Partition at /boot(that is within the chroot, so mount it as /mnt/boot with mount /dev/sdXY /mnt/boot) &quot;</p> <p>and can&#39;t use “<a href="https://wiki.archlinux.org/title/Mac#Using_the_native_Apple_boot_loader_with_GRUB">5.3 Using the native Apple boot loader with GRUB</a>” since there is no more apple file/os left on the drive. (not sure if my understanding is correct.)</p> <p>using dd to the archlinux.ios. and format the usb to fat32 first.<br/> Please help. Thank you in advance.</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/JohnSanDeigo"> /u/JohnSanDeigo </a> <br/> <span><a href="https://www.reddit.com/r/archlinux/comments/131l7oo/anyone_successfully_install_archlinux_on/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/archlinux/comments/131l7oo/anyone_successfully_install_archlinux_on/">[comments]</a></span>