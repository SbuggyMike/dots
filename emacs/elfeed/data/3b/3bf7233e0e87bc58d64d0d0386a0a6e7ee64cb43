<!-- SC_OFF --><div class="md"><p><strong>Distro</strong>: i am using manjaro i3 community version <strong>what i want</strong>: and i want to execute the feh command to set wallpapers from my wallpapers directory randomly and i use the watch command to repeat that process <em>after every x amount of time</em> but apparently systemd does not support wildcards</p> <p><strong>systemd user</strong> i am using the systemd for the current user with the <code>--user</code> flg for anyone wondering about it.</p> <p><strong>solutions i tried</strong>: 1. i tried to escape it by using the command line tool <code>systemd-escape</code> but it has the same problem i think systemd first converts the escaped characters back to the normal and then executes it and the problem comes back does anyone knows how to execute the wildcards in any other way</p> <ol> <li>i have tried to execute it by using a shell script and then execute the shell script but it does nothingi also tried to do the <code>ExecStart=/bin/bash -c &#39;watch -n 5 feh --randomize --bg-fill ~/wallpapers/* &gt; /dev/null&#39;</code> it didn&#39;t worked| <strong>command i desire to execute</strong>: <code>watch -n 6 feh --randomize --bg-fill ~/Dotfiles/wallpaper/* &gt; /dev/null</code> this is the command i wanted to execute</li> </ol> <p><strong>Note</strong> If i use the <code>feh --bg-fill /path/to/wallpaper</code> it sets the wallpaper i want to use the <code>--randomize</code> flag to change wallpapers randomly thats why i need the wildcard</p> </div><!-- SC_ON --> &#32; submitted by &#32; <a href="https://www.reddit.com/user/theSafdarAwan"> /u/theSafdarAwan </a> <br/> <span><a href="https://www.reddit.com/r/archlinux/comments/wb3ezo/help_systemd_wildcards_problem/">[link]</a></span> &#32; <span><a href="https://www.reddit.com/r/archlinux/comments/wb3ezo/help_systemd_wildcards_problem/">[comments]</a></span>